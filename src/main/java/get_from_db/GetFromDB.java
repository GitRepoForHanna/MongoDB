package get_from_db;

public class GetFromDB {



    public static void main(String[] args) {
//        MongoCollection collection = DataBaseContext.getCollection("kittens", "cats");
//
//        MongoCursor cursor = collection.find(Filters.eq("age",3)).iterator();
//        List<Bson> filters = new ArrayList<>();
//        filters.add(Filters.eq("age", 3));
//        filters.add(Filters.eq("name", "Druzhok"));
//        System.out.println(new Gson().toJsonTree(collection.find(Filters.and(filters)).first()).getAsJsonObject().get("color"));
//        System.out.println(collection.countDocuments(Filters.eq("age", 3)));
//        while (cursor.hasNext()) {
//            System.out.println(cursor.next());
//        }
//
//        List<JsonObject> result = DataBaseContext.readDocuments(collection, Filters.and(filters));
//        result
//                .forEach(jsonObject -> {
//                    System.out.println(CatConverter.readFromJson(jsonObject).getName());
//                });
    }

}
